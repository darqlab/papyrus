<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <parent>
        <groupId>io.darqlab</groupId>
        <artifactId>papyrus</artifactId>
        <version>0.1.0-SNAPSHOT</version>
    </parent>

    <artifactId>papyrus-ingestor</artifactId>
    <name>Papyrus :: Ingestor</name>
    <description>
        On-demand batch reingest CLI (c-ingestor, ADR-009) — normally-off entry point that
        bootstraps a blue-green per-embedding-model database and (in a later task) reingests
        the corpus from filesystem archives under the target model. Reuses papyrus-pipeline
        and papyrus-extractor; no new domain logic.
    </description>
    <packaging>jar</packaging>

    <dependencies>

        <!-- Internal -->
        <dependency>
            <groupId>io.darqlab</groupId>
            <artifactId>papyrus-core</artifactId>
        </dependency>
        <dependency>
            <groupId>io.darqlab</groupId>
            <artifactId>papyrus-extractor</artifactId>
        </dependency>
        <dependency>
            <groupId>io.darqlab</groupId>
            <artifactId>papyrus-pipeline</artifactId>
        </dependency>

        <!-- Spring (batch CLI — no web/MCP starters) -->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter</artifactId>
        </dependency>
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-jdbc</artifactId>
        </dependency>
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-configuration-processor</artifactId>
            <optional>true</optional>
        </dependency>

        <!-- Database driver + migrations (also transitive via papyrus-pipeline; declared
             explicitly because this module talks to Postgres/Flyway directly) -->
        <dependency>
            <groupId>org.postgresql</groupId>
            <artifactId>postgresql</artifactId>
            <scope>runtime</scope>
        </dependency>
        <dependency>
            <groupId>org.flywaydb</groupId>
            <artifactId>flyway-core</artifactId>
        </dependency>
        <dependency>
            <groupId>org.flywaydb</groupId>
            <artifactId>flyway-database-postgresql</artifactId>
        </dependency>

        <!-- Test -->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-test</artifactId>
            <scope>test</scope>
        </dependency>
        <dependency>
            <groupId>org.testcontainers</groupId>
            <artifactId>postgresql</artifactId>
            <scope>test</scope>
        </dependency>
        <dependency>
            <groupId>org.testcontainers</groupId>
            <artifactId>junit-jupiter</artifactId>
            <scope>test</scope>
        </dependency>

    </dependencies>

    <build>
        <plugins>
            <plugin>
                <groupId>org.springframework.boot</groupId>
                <artifactId>spring-boot-maven-plugin</artifactId>
            </plugin>
        </plugins>
    </build>

</project>
